language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t rajgandhi0011/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true rajgandhi0011/docker-react npm run test